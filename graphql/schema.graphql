type User {
    id: ID!
    name: String
    online: Boolean
    typing: Boolean
    lastOnlineUpdate: AWSTimestamp
    lastTypingUpdate: AWSTimestamp
}

input CreateUserInput {
    name: String
}

input UpdateUserInput {
    online: Boolean
    typing: Boolean
    lastOnlineUpdate: AWSTimestamp
    lastTypingUpdate: AWSTimestamp
}

type Query {
    listUsers: [User]
}

type Mutation {
    createUser(user: CreateUserInput!): User
}
